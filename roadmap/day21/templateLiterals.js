// template literals: template strings
// it allows us to embed or insert values or expressions or variables inside the js strings.
// they are enclosed within backticks(``)
// we can insert the expressions or variables or values in the string wherever we used ${} literal.
// let name = 'sathish';
// let age = 25;

// let message = `hello, my name is ${name} and my age is ${age}`;

// console.log(message);

// console.log(`the sum of ${5} and ${6} is ${5 + 6}`);

let message = `hello
world`;

console.log(message);